# Linq Partner API
LINQ_API_KEY=
LINQ_FROM_NUMBER=+15555550100
LINQ_WEBHOOK_SECRET=

# Dockhand
DOCKHAND_BASE_URL=http://localhost:3000
DOCKHAND_TOKEN=

# OpenAI (via ai-sdk)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
# Optional: point at a compatible endpoint (Azure, OpenRouter, local LM, etc.)
# OPENAI_BASE_URL=https://api.openai.com/v1
# Agent call timeout (ms) — aborts generateText if OpenAI hangs
AGENT_TIMEOUT_MS=60000

# Allowlist (comma-separated E.164)
ALLOWED_SENDERS=+15555550100

# Prisma
DATABASE_URL=file:./dev.db

# Server
PORT=4000
